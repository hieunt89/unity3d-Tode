public class Events {
	public enum Input{
		EMPTY_SELECT,
		ENTITY_SELECT,
		ENTITY_DESELECT,
		TOWER_SELECT
	}

	public enum Game{
		GOLD_CHANGE,
		LIFE_CHANGE,
		TIME_CHANGE
	}

	public enum Loading{
		DONE_INIT
	}
}
